<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    tools:context=".views.main_screens.MainScreenActivity"
    tools:showIn="@layout/activity_main_screen">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/mainToolbarAppBarLayout"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@color/colorPrimary"
        android:theme="@style/ToolbarWhiteColouredBackArrow">

        <com.iguerra94.weathernow.views.toolbar.SimpleToolbar
            android:id="@+id/mainToolbar"
            app:titleTextAppearance="@style/Toolbar.TitleText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:layout_margin="8dp"
            android:elevation="4dp"
            />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Resto de la interfaz de usuario -->
    <FrameLayout
        android:id="@+id/main_content_screen_fragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />

</LinearLayout>